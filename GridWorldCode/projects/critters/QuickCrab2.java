if (getLocation().getAdjacdentLocation(cdir-90).getAdjacentLocation(cdir-90) != null && getLocation().getAdjacdentLocation(cdir+90).getAdjacentLocation(cdir+90 != null){
	// everything 2 away is full
	if (getLocation().getAdjacentLocation(cdir-90) 
}
